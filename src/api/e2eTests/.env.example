# Environment variables for E2E tests
# Copy this file to .env and fill in your actual values

# MongoDB Configuration (Required for E2E tests)
# The MongoDB connection string for your test database
MONGODB_URI=mongodb://localhost:27017

# The name of the MongoDB database to use for testing
# It's recommended to use a separate database for testing
DATABASE_NAME=csharp_ai_buddy_test

# Optional: OpenAI API Key (if tests require AI functionality)
# OPENAI_API_KEY=your_openai_api_key_here

# Optional: Test environment indicator
ENVIRONMENT=test

# Note: 
# - Make sure your test database is separate from production
# - The e2e tests will create and delete test data automatically
# - Ensure MongoDB is running and accessible at the specified URI